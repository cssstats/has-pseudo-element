var assert = require('assert');
var hasPseudoElement = require('..');

describe('has-pseudo-element', function() {

  it('should do something awesome', function() {
    assert.equal(hasPseudoElement(), true);
  });
});
